<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="../src/output.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <% if (messages.error) { %>
        <script>
          alert('<%= messages.error %>');
        </script>
      <% } else if (messages.success) { %>
        <script>
          alert('<%= messages.success %>');
        </script>
      <% } %> 
      <div class="container mx-auto p-4">
        <h1 class="text-2xl font-bold mb-4"><%= user.company %></h1>
        <h1 class="text-2xl font-bold mb-4">Post a job</h1>
        <form action="/recruiters/postajob" method="post" enctype="multipart/form-data" class="space-y-4">
          <div class="flex flex-col">
              <label for="title" class="text-lg font-semibold">Title</label>
              <input type="text" name="title" id="title" class="border border-gray-300 px-4 py-2 rounded-lg focus:outline-none focus:border-blue-500">
          </div>
          <div class="flex flex-col">
              <label for="salary" class="text-lg font-semibold">Salary or LPA</label>
              <input type="text" name="salary" id="salary" class="border border-gray-300 px-4 py-2 rounded-lg focus:outline-none focus:border-blue-500">
          </div>
          <div class="flex flex-col">
              <label for="company" class="text-lg font-semibold">Company</label>
              <input type="text" name="company" id="company" class="border border-gray-300 px-4 py-2 rounded-lg focus:outline-none focus:border-blue-500">
          </div>
          <div class="flex flex-col">
              <label for="location" class="text-lg font-semibold">Location</label>
              <input type="text" name="location" id="location" class="border border-gray-300 px-4 py-2 rounded-lg focus:outline-none focus:border-blue-500">
          </div>
          <div class="flex flex-col">
              <label for="description" class="text-lg font-semibold">Description</label>
              <input type="text" name="description" id="description" class="border border-gray-300 px-4 py-2 rounded-lg focus:outline-none focus:border-blue-500">
          </div>
          <div class="flex flex-col">
              <label for="jobdescription" class="text-lg font-semibold">Job Description</label>
              <input type="file" name="jobdescription" id="jobdescription" class="border border-gray-300 px-4 py-2 rounded-lg focus:outline-none focus:border-blue-500">
          </div>

          <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded-lg hover:bg-blue-600">Post</button>
      </form>
        <div class="mt-4"> 
            <form action="/recruiters/home?_method=GET" method="post">
                <button class="bg-blue-500 text-white px-4 py-2 rounded-lg hover:bg-blue-600">Home page</button>
            </form>
        </div>
    </div>
    
</body>
</html>
